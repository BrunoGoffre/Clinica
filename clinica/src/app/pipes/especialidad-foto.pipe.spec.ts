import { EspecialidadFotoPipe } from './especialidad-foto.pipe';

describe('EspecialidadFotoPipe', () => {
  it('create an instance', () => {
    const pipe = new EspecialidadFotoPipe();
    expect(pipe).toBeTruthy();
  });
});

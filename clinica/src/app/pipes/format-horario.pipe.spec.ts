import { FormatHorarioPipe } from './format-horario.pipe';

describe('FormatHorarioPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatHorarioPipe();
    expect(pipe).toBeTruthy();
  });
});
